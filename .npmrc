; LangChain's @langchain/community declares an optional `better-sqlite3 <12` peer that conflicts with the
; version pulled transitively (we don't use LangChain's SQLite store), so we relax peer-dep resolution.
legacy-peer-deps=true
